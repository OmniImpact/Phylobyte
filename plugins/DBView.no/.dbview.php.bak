<?php

//connect to db, return error if failed, otherwise, we are ready to continue

if(!is_file('../data/database.vars.php')){
	$this->messageAddError('Could not find database variables file, please create it.');
}else{
include('../data/database.vars.php');
}

try{
	$TEST_PDO_CONNECT = new PDO('mysql:host=localhost;dbname='.$DBMASTERDB, $DBUSER, $DBPASSWORD);
}catch(Exception $exception){
	$this->messageAddError($exception->getMessage());
}

?>
